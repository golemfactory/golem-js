export const GLOBAL_DEFAULTS = Object.freeze({
  // ?  How about ranaming?  this is url of registry of images not the repo
  // ( at least in registry.golem.network language repo is set of images )

  repoUrl: "https://registry.golem.network",
  payment: { driver: "erc20", network: "goerli" },
});
