export const GLOBAL_DEFAULTS = Object.freeze({
  //TODO : conider ranaming, this is url of registry of images not the repo
  repoUrl: "http://registry.dev.golem.network",
  payment: { driver: "erc20", network: "goerli" },
});
