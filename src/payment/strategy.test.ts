describe("SDK provided Payment Filters", () => {
  describe("acceptAllDebitNotesFilter", () => {
    it.todo("Accepts all debit notes");
  });

  describe("acceptAllInvoicesFilter", () => {
    it.todo("Accepts all invoices");
  });

  describe("acceptMaxAmountDebitNoteFilter", () => {
    it.todo("Accepts debit notes which don't exceed a specified amount");
  });

  describe("acceptMaxAmountInvoiceFilter", () => {
    it.todo("Accepts invoices that don't exceed a specified amount");
  });
});
